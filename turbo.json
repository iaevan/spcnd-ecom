{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"],
      "env": ["SPCND_TEST_PG_URL", "SPCND_TEST_MYSQL_URL"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
